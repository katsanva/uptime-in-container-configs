FROM node:onbuild

MOUNT .:/src
WORKDIR /src
RUN npm install
VOLUME /root/.npm

FROM node:slim

ENV SVC_PATH /var/service
ENV MONGODB_CONNECT 172.17.0.1:27017
ENV AUTH_USER root
ENV AUTH_PASSWORD root

COPY . $SVC_PATH

CMD cd $SVC_PATH && node app

EXPOSE 8082

